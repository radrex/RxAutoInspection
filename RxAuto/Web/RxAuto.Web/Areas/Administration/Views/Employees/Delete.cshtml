@model RxAuto.Web.ViewModels.Employees.ViewModels.DeleteEmployeeViewModel

@{
    ViewData["Title"] = "Delete";
}

<div class="container">
    <div class="row">
        <div class="col">
            <h1 class="d-flex justify-content-center">Изтриване на служител</h1>
            <hr />
            <h3>Сигурни ли сте, че искате да изтриете този служител? Всяка работна позиция и всяка работна локация ще загубят изтрития служител.</h3>
        </div>
    </div>
</div>

<div class="container">
    <div class="row">
        <div class="col-12">
            <hr />
            <dl class="row">
                <dt class="col-sm-2">
                    <label asp-for="@Model.FullName"></label>
                </dt>
                <dd class="col-sm-10">
                    <label asp-for="@Model.FullName">@Model.FullName</label>
                </dd>
                <dt class="col-sm-2">
                    <label asp-for="@Model.JobPosition"></label>
                </dt>
                <dd class="col-sm-10">
                    <label asp-for="@Model.JobPosition">@Model.JobPosition</label>
                </dd>
                <dt class="col-sm-2">
                    <label asp-for="@Model.OperatingLocation"></label>
                </dt>
                <dd class="col-sm-10">
                    <label asp-for="@Model.OperatingLocation">@Model.OperatingLocation</label>
                </dd>
            </dl>

            <form method="post" asp-area="Administration" asp-controller="Employees" asp-action="Delete">
                <input type="submit" value="Изтрий" class="btn btn-danger" />
                <a class="btn btn-info" asp-area="Administration" asp-controller="Employees" asp-action="All">Върни се обратно</a>
            </form>
        </div>
    </div>
</div>
